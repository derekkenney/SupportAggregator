//This is the Sample for the cure config file. Update it and rename is "secret-cure-config.json" 
//then save it in the same directory as the js files.

{
	"userName": "user",
	"password": "password",
	"server": "database",
	"options":{
		"port": "8080",
		"database": "mydb"
	},
	"debug":{
		"packet": "true",
		"data": "true",
		"payload": "true",
		"token": "true",
		"log": "true"
	}
}