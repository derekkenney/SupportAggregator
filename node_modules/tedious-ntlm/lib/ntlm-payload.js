// Generated by CoffeeScript 1.6.2
var FLAGS_1, FLAGS_2, FLAGS_3, NTLMResponsePayload, TYPE_FLAGS, WritableTrackingBuffer, libraryName, os, sprintf, versions;

WritableTrackingBuffer = require('./tracking-buffer/writable-tracking-buffer');

require('./buffertools');

os = require('os');

sprintf = require('sprintf').sprintf;

crypto = require('crypto');

libraryName = require('./library').name;


versions = require('./tds-versions').versions;
var BigInteger = require('bignumber').BigInteger;

NTLMResponsePayload = (function() {

  function NTLMResponsePayload(loginData) {
  
    //console.log("Preparing a response");
    var responseBuffer = loginData.ntlmpacket.createResponse(loginData.domainName, loginData.userName, loginData.password);
	this.data = responseBuffer;
	
  };
	

  NTLMResponsePayload.prototype.toString = function(indent) {
    indent || (indent = '');
    return indent + 'NTLM Auth';
  };

  return NTLMResponsePayload;

})();

module.exports = NTLMResponsePayload;
